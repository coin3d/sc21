{
    IBClasses = (
        {
            ACTIONS = {
                dumpSceneGraph = id; 
                open = id; 
                showDebugInfo = id; 
                toggleHeadlight = id; 
                viewAll = id; 
            }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {coincontroller = SCController; filenametext = NSTextField; view = SCView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}