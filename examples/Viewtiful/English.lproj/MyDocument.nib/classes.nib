{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {addViewpoint = id; dumpScenegraph = id; }; 
            CLASS = MyDocument; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSDocument; 
        }, 
        {
            CLASS = MyExaminerController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = SCExaminerController; 
        }, 
        {
            ACTIONS = {
                documentChanged = id; 
                dumpScenegraph = id; 
                modeButtonClicked = id; 
                modeMenuSelected = id; 
                showDebugInfo = id; 
            }; 
            CLASS = MyWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                controller = SCExaminerController; 
                modebutton = NSButton; 
                sizetext = NSTextField; 
                typetext = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}