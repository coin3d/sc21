{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {viewAll = id; }; 
            CLASS = MyCoinController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = SCController; 
        }, 
        {
            ACTIONS = {addViewpoint = id; dumpScenegraph = id; }; 
            CLASS = MyDocument; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSDocument; 
        }, 
        {
            ACTIONS = {
                documentChanged = id; 
                dumpScenegraph = id; 
                modeButtonClicked = id; 
                modeMenuSelected = id; 
                showDebugInfo = id; 
            }; 
            CLASS = MyWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                coinhandler = SCEventHandler; 
                controller = SCController; 
                examinerhandler = SCEventHandler; 
                modebutton = NSButton; 
                sizetext = NSTextField; 
                typetext = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}